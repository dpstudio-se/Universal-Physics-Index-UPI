# Visual / Conceptual Map → UPI

This document maps the supplied visual concepts into UPI without treating visual
similarity as physical proof. Each mapping receives an evidence class.

## 1. Information-flow / Nordic historical network

Visual concept:
- migration, exchange, routes, settlements, chronology
- information moving through a geographic network

UPI representation:
`G=(V,E,w,t)`

- `V`: places, populations or archaeological sites
- `E`: documented connection/route
- `w`: evidence weight or confidence
- `t`: dated interval

Classification:
- EST: individual historical/archaeological facts only when sourced.
- DER: graph statistics calculated from sourced data.
- HYP: claims that a universal physical frequency drives historical migration.
- SYM: Ω1766/TF1766 visual language.

Guard:
A historical interval is not automatically a frequency in hertz. Any mapping
from chronology to Hz must be an explicit model transformation with units and
uncertainty.

## 2. Local-universe / observational geometry visual

UPI representation:
`OBSERVATION → COORDINATE/IMAGE DATA → GEOMETRY MODEL → TEST`

Classification:
- EST: measured image/telescope data when source metadata exists.
- DER: geometric measurements derived from the data.
- HYP: larger cosmological interpretation.
- SYM: Vortex visual analogy.

Guard:
The visual shape itself is not evidence for a new cosmological geometry.

## 3. Universal rotary / vortex mechanism

Proposed abstract pipeline:

`oscillation → phase → rotation → flow → structure`

UPI treatment:
- `phase`: measurable/modelled phase variable
- `rotation`: angular velocity `omega=2*pi*f`
- `flow`: velocity field when a physical model is specified
- `structure`: graph/manifold/state representation

The existing `omega1766.py` nine-node oscillator is one explicit toy model.
It must not be presented as a universal mechanism.

## 4. 7.834125 Hz ↔ 8 Hz visual

Declared inputs:

`f0=7.834125 Hz`
`f1=8.0 Hz`

Derived:

`Delta f=0.165875 Hz`
`Tbeat=1/Delta f≈6.028636 s`
`omega0=2*pi*f0≈49.22326 rad/s`
`omega1=2*pi*f1≈50.26548 rad/s`

Classification:
- DER: arithmetic consequences of the declared frequencies.
- HYP: any proposed causal or biological coupling between them.
- EST: Schumann resonance is an observed geophysical electromagnetic phenomenon,
  but its instantaneous/representative frequency is not an exact universal
  7.834125 Hz constant.

Guard:
Do not convert a frequency difference into a causal mechanism without an
independent observable and null model.

## 5. Spiral-flow simulation visual

UPI representation:

`f(t) → omega(t) → phase(t) → velocity field → particle trajectories → residuals`

The existing dynamic engine and `DNAReader.analyze_dynamic()` already enforce
declared inputs, source provenance and software-test classification.

Control:
`phase[n+1]=phase[n]+omega[n] Delta t`

For any proposed flow law, preserve dimensions and independently test the
numerical integration.

## 6. Information → metric → stability → geometry

A rigorous mathematical bridge begins with a probability model:

`p(x|theta)`

and Fisher information:

`g_ij^F = E[(∂_i log p)(∂_j log p)]`

which defines a metric on parameter space under the usual regularity
conditions.

For pure quantum states, the Fubini–Study metric provides a geometry on
projective Hilbert space.

UPI representation:

`INFORMATION → METRIC → STABILITY → GEOMETRY → TEST`

Classification:
- EST/DER: the mathematical definitions themselves.
- HYP: the stronger statement that spacetime is universally generated by
  information stabilization.
- HYP: any direct identification of entanglement geometry with a specific
  Ω7834/Ω1766 mechanism.

Guard:
Do not infer spacetime emergence from Fisher/Fubini–Study geometry alone.

## 7. Trojan / resonance control case

Use a real dynamical system as a separate control family:

`three-body gravity → L4/L5 equilibrium → libration → observation`

Compare against the Vortex graph only after deriving both independently.

Required separation:
- Trojan 1:1 orbital resonance is established celestial mechanics.
- Vortex/Laplacian resonance is a mathematical model.
- A claimed equivalence between them is HYP and must pass Mirror/Noise/Falsify.

## 8. Unified UPI routing

```text
VISUAL / RAW DATA
       ↓
OBSERVATION
       ↓
SOURCE + UNITS + TIME
       ↓
CLASSIFY: EST / DER / HYP / SYM / ERR / STOP
       ↓
MODEL
       ↓
TEST GENERATOR
       ↓
VM CHAMBER
       ↓
T3MP3ST
  ├─ SHADOW
  ├─ MIRROR
  └─ NOISE
       ↓
ESCAPE DETECTOR
       ↓
EVIDENCE
       ↓
RETEST / FALSIFY
       ↓
VALIDATION GATE
  ├─ PROPOSAL
  └─ STOP
       ↓
DNA UPDATE PROPOSAL
       ↻
```

## 9. DNA / RNA ownership

DNA / reference:
- sourced physics
- sourced historical observations
- mathematical definitions
- provenance
- units
- prior validated software relations

RNA / dynamic:
- simulations
- parameter sweeps
- test generation
- residual analysis
- hypothesis exploration

Bridge:
- validation gate
- provenance checks
- dimensional checks
- falsification status

No visual concept is allowed to bypass the bridge and become established
knowledge.

## 10. Next measurable targets

1. Build an information-geometry module for Fisher metrics.
2. Build a three-body Trojan control module independently of the Vortex graph.
3. Feed both into the same T3MP3ST interface.
4. Compare invariants rather than pictures.
5. Store only traceable results as DNA proposals.
