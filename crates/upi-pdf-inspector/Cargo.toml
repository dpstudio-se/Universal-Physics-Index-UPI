[package]
name = "upi-pdf-inspector"
version = "0.1.0"
edition = "2021"
description = "Deterministic PDF inspection and evidence extraction adapter for Universal Physics Index"
license = "MIT"

[dependencies]
lopdf = "0.35"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
